{
  "name": "prettier-plugin-advent",
  "private": true,
  "main": "target/plugin.js",
  "scripts": {
    "build": "esbuild src/plugin.ts --bundle --platform=node --outdir=target/ --log-level=error"
  },
  "devDependencies": {
    "esbuild-wasm": "^0.27.0",
    "prettier": "^3.4.1"
  },
  "peerDependencies": {
    "prettier": "^3.0.0"
  }
}
