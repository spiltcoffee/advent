{
  "name": "prettier-plugin-advent",
  "private": true,
  "main": "target/plugin.js",
  "scripts": {
    "build": "esbuild src/plugin.ts --bundle --platform=node --outfile=target/plugin.js --log-level=error"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "prettier": "^3.4.1"
  },
  "peerDependencies": {
    "prettier": "^3.0.0"
  }
}
